// Brand palette — raw hex values for use in inline SVG strokes and `style` props
// where a Tailwind class can't reach (e.g. dynamic colours, SVG `stroke`). For
// className usage prefer the Tailwind tokens in globals.css (bg-oxblood,
// text-marine, …) — these are the same values, kept in one place so the dashboard
// and plan views don't drift.

export const OXBLOOD = '#8c2b2b';
export const MARINE  = '#14617e';
export const FERN    = '#4f7a52';
export const GOLD    = '#8f6512';
export const AMBER   = '#dfa01c';
export const FOG     = '#d9d3c6';
export const BONE    = '#f4efe4';
